#!/usr/bin/env bash
#MISE description="Remove rendered Ignition files and stale secrets staging dir"
set -euo pipefail
rm -f provisioning/servers/*/materia.ign provisioning/servers/*/materia-debug.ign
rm -rf "$PWD/secrets"
