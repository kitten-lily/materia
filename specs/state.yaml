workflow_mode: solo-git
session:
  active_flow: build_epic
  active_epic_id: e01
  active_story_id: e01s06
git:
  branch: main
  base: main
handoff:
  next_skill: plan-work
  note: "e01s05 (quadlet resources) merged to main (836a273), 2 commits ahead of origin/main, not pushed. Next: e01s06 (component MANIFEST.toml — Secrets, Services, Defaults, incl. adding hcPingURL + resticPassword attributes to attributes/flutterina.yml via sops edit)."
epic_cycle:
  step: 0
  current_story: e01s06
  audit_result: pending
metrics: {}
